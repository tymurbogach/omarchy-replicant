# G7 offscreen screenshots

`test-g7-screenshots.sh` renders these fixtures with the Omarchy shell and
compares every pixel exactly. The QML harness uses only `fixtures/g7/*.json`
and does not read the user's repository or configuration.
